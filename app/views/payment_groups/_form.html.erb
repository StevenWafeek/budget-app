<%= form_with(model: payment_group) do |form| %>
  <% if payment_group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(payment_group.errors.count, "error") %> prohibited this payment_group from being saved:</h2>

      <ul>
        <% payment_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :payment_id, style: "display: block" %>
    <%= form.text_field :payment_id %>
  </div>

  <div>
    <%= form.label :group_id, style: "display: block" %>
    <%= form.text_field :group_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
