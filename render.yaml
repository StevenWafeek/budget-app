databases:
  - name: micronaut
    databaseName: micronaut_sntz
    user: micronaut_sntz_user

services:
  - type: web
    name: stev
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: micronaut
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
